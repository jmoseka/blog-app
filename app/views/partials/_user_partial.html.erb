<%= stylesheet_link_tag('user_partial') %>

<section>
<%= image_tag('avatal.jpg', class:'avatar')%>

<div>
<h2><%= link_to(user.name, user_path(user))%></h2>

<p>
Number of Posts : <%= user.posts_counter%>
</p>
</div>
</section>